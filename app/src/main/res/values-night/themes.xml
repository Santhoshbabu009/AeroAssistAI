<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AeroAssistAI" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Dark Theme Color Palette -->
        <item name="colorPrimary">@color/accent_aero</item>
        <item name="colorPrimaryDark">@color/slate_dark</item>
        <item name="colorAccent">@color/accent_aero</item>
        <item name="android:windowBackground">@color/slate_dark</item>
        <item name="android:statusBarColor">@color/slate_dark</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>